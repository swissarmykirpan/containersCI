machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/.cache/docker"
  override:
    - docker info
    - docker build -t gusohal/meteor:latest .
    - bundle install

test:
  override:
    - bundle exec rake spec:meteor
