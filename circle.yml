machine:
  ruby:
    version: 2.1.5
  services:
    - docker

dependencies:
  cache_directories:
    - "~/.cache/docker"
  override:
    - docker info
    - docker build -t gusohal/circlecitest .
    - ls
    - bundle install

test:
  override:
    - rspec
