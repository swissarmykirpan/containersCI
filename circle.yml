machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/.cache/docker"
  override:
    - docker info
    - docker build -t gusohal/meteor:latest .
    - bundle install


deployment:
  dockerhub:
    branch: master
    commands:
      - $TRIGGER_DOCKER_HUB_BUILD
