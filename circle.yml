machine:
  ruby:
    version: 2.1.5
  services:
    - docker

dependencies:
  cache_directories:
    - "~/.cache/docker"
  override:
    - docker info
    - docker build -t gusohal/circlecitest .
    - ls
    - cd ..
    - ls
    - bundle install

test:
  override:
    - bundle exec rspec
