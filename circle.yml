machine:
  ruby:
    version: 2.1.5
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t gusohal/circlecitest
    - bundle install


test:
  override:
    - bundle exec rspec
