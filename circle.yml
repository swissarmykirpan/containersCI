machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/.cache/docker"
  override:
    - docker info
    - docker build -t gusohal/circlecitest .
    - ls
    - bundle install

test:
  override:
    - rspec spec/Dockerfile_spec.rb
